var label = "even" if x % 2 == 0 else "odd"

# Chains as: "small" if n < 10 else ("large" if n > 100 else "medium")
var size = ("small" if n < 10
            else "large" if n > 100
            else "medium")
