var a = 2, 3          # tuple without parentheses
var b = (2, 3)        # same tuple with parentheses
var x, y = b            # x is 2, y is 3
()                      # empty tuple
(1,)                  # one-element tuple — the trailing comma matters
var point = (10, 20)
print(point[0])        # 10 — tuples support indexing
